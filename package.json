{
  "name": "epump-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone Playwright test suite for ePump portal",
  "scripts": {
    "test": "playwright test",
    "test:list": "playwright test --list",
    "test:smoke": "playwright test tests/01-login.spec.ts tests/14-BankValidation.spec.ts --project=\"Google Chrome\" --workers=1",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@types/node": "^25.5.0"
  }
}
